# docker-syslog-retention
Simple container that provides the ability to remove logfiles older then 90 days.

## architectures
This image is currently available for `linux/arm64` and `linux/amd64`.

## how to use
See the `examples` directory for some ideas how this stuff can be used.
